/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ["via.placeholder.com"], // Add other allowed hostnames as needed
      },
};

export default nextConfig;
